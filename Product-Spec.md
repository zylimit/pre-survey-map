# Product Spec: Pre-Survey Map

> 站点勘测数据统一管理平台
> 版本：V1.0 · 日期：2026-05-28

---

## 产品说明

一个 B/S 架构的站点勘测数据统一管理平台，**唯一用户是勘测数据维护工程师**。

工程师每周把多源勘测数据（Google Earth 手工标记的 KMZ + 内部系统 ISDP / eSurvey 导出的 Excel）导入平台，做去重和冲突处理后入库；需要派工时，在地图上框选目标区域导出该区域 KMZ，通过邮件发给一线勘测员；勘测员用 Google Earth 打开 KMZ 直接看到任务位置去现场。

**核心价值**：统一散落在多个系统和手工标记里的勘测数据，**避免勘测员跑到已经勘测过的点上做重复工作**。

**用户**：
- 直接用户：**勘测数据维护工程师**（每周一次大批量维护 + 不定期派工，约 1-2 小时/次）
- 间接受益：**一线勘测员**（不直接使用平台，通过邮件接收 KMZ，用 Google Earth 查看）

---

## 功能需求

| ID | 功能 | 说明 |
|----|------|------|
| F1 | 单文件导入 | 支持 `.kml` / `.kmz` / `.xlsx`，点击按钮选择 + 拖拽到地图区；**V1 只允许一次一个文件**（拖入多个仅接受第一个并 warn）；单文件上限 100MB |
| F2 | 多源数据解析 | KML/KMZ 解析 Site / Road / Lessor 三类；Excel 解析 Site 详表（含 50 列扩展字段）；**表头行自动探测（V1.x #35）**：扫描前若干行定位含 `SITE ID` 的表头行，**兼容旧模板（第 1 行分类横幅 + 第 2 行字段名）与新模板（第 1 行直接字段名、含 OPERATOR/SITE CATEGORY/SITE TYPE 盖戳列）** 两种布局，不再写死行号；盖戳列源值仍按 F20 忽略 |
| F3 | 冲突检测 | 按"大小写不敏感 + 去首尾空格 + 字符串比较"规则比对名称字段；同类比同类（点 vs 点、面 vs 面） |
| F4 | 冲突处理（步骤 2）| 数据清洗（F13）完成后进入；强制弹冲突列表对话框，不处理不让继续；单条/批量决策：覆盖 / 忽略（仅导无冲突） / 取消（导出冲突 Excel 自行修改） |
| F5 | 冲突列表导出 | 当用户选"取消"时，导出冲突明细为 Excel，含库内外字段双方对照 + 来源文件名 |
| F6 | 地图可视化 | OpenLayers 渲染；CartoDB Positron 默认底图，可切 OSM / Esri 卫星 / Google 卫星（开发调试 + 内部服务器 beta 部署均走未授权 tile 接口临时方案；仅公有云商用部署时替换为 Google Maps API Key）|
| F7 | 图层树管理 | ~~左侧单层树，三个固定文件夹：Site / Road / Lessor；节点带数量 + 复选框显隐 + 顶部搜索框；列表虚拟化~~ **【V1.x #24 · F20 取代】树结构重构为固定深层图层体系（运营商→类别→类型），单要素不再进树而是落入「查看图层要素」列表框；本行原浅树描述作废，详见 F20**|
| F8 | 属性查看 | 点击地图要素或树节点 → 右侧面板平铺显示该要素**所有字段**（V1 不分 tabs）；可 ✖ 关闭，**关闭后无常驻入口**，再次点击要素或树节点自动重新打开 |
| F9 | 框选导出 KMZ | 工具栏 [框选模式 ▾] 选自由多边形 / 矩形 → 在地图绘制 → 导出严格包含在选区内的所有要素 |
| F10 | 整库导出 KMZ | 工具栏 [导出 KMZ ▾] 下拉：[导出整库] / [导出选区] |
| F11 | 操作反馈 | 底部可折叠输出面板：进度条 + 操作日志 + 错误警告 + 数据库连接状态 |
| F12 | 自动定位（双向焦点同步）| 导入完成自动 fit bounds 到新导入的数据范围；**点击树节点 → 地图飞到该要素并放大**；**地图选中要素 → 左树自动展开所在文件夹 + 滚动到该节点并高亮**（虚拟化列表下需按行号计算 scrollTop）|
| F13 | 数据清洗向导（步骤 1）| 解析后扫描脏数据 4 类：坐标写反 / 漏小数点 / 在海里 / 不在主基准；按类型分配默认动作；用户逐条或批量决策 [自动修复] / [保留] / [丢弃]；顶部 banner 显示**主基准区域 + 文件内自冲突去重摘要**；完成后进入步骤 2 冲突检测 |
| F14 | 清除基线（调试用 + 换基线唯一通道）| Toolbar 加 [🗑️ 清除基线] 按钮 → 弹确认 modal → 后端 truncate `site` / `road` / `lessor` + **`baseline_state`** 四表；**这是用户换基线国家的唯一方式**（V1 不做基线漂移、不做工作空间）；V1 不做权限控制 |
| F15 | 全局基线状态栏（V1.x #15）| Toolbar 下方独立一条 28px 横条，永远显示当前基线状态："📍 基线国家：菲律宾 (PH) · 固化于 2026-05-29" 或 "📍 基线未确立 · 首次导入完成后将自动锁定"|
| F16 | 全局搜索结果列表（V1.x #16）| Toolbar 右上搜索框回车 → **三类要素全搜（显示名匹配，与左树过滤同口径）**；结果写入底部 Output 的**独立"搜索结果区"**（不挤占 50 条日志），封顶 200 条；区头可点汇总条"🔍 搜索匹配 N 条，飞到第一条" + **[✖ 清空结果] 按钮**；每条结果一行 = **显示名 · 核心属性 · 类型角标**，可点 → **地图飞到该要素 + 左树该节点同步高亮滚动定位**（复用 F12 双向焦点同步）|
| F18 | 双语界面（中/英）（V1.x #21）| 默认英文；Toolbar 右上 **[EN/中]** 切换按钮；**仅切换软件自身 UI 文案**（按钮/标签/对话框/状态栏/输出日志/错误提示/树节点文件夹名/底图切换标签/基线国家名/LayerTree tooltip 与 placeholder），导入文件内容、数据字段名及字段值**不翻译**；偏好存 `localStorage`（key: `presurvey.lang`）；菲律宾工程师用英文，中国工程师可切回中文；后端 `GET /api/baseline-state` 同时返回 `name_zh` + `name_en`，前端按当前语言选择显示 |
| F17 | 基线恢复点与回滚（V1.x #20）| **不可逆操作的安全网**。导入 commit 前 / 清除基线（F14）前 / 回滚前 **自动建恢复点**（快照 `site`/`road`/`lessor` 三表 + `baseline_state`），并提供 Toolbar [🕘 恢复点] 手动建点；保留最近 N=10 个（环形淘汰最旧）；恢复点对话框列表可**覆盖式回滚**到任一点（事务内 truncate + 从快照重灌，回滚本身也先自动建点 → 可逆）+ 快捷 [↩ 撤销上一次导入]；详见「基线恢复点与回滚机制（F17）」节 |
| F20 | 图层体系（V1.x #24）| **替换 F7 浅树**为固定深层图层树（菲律宾 = Globe / Smart / Dito 三运营商写死，不会增加）。三种节点：📁 **文件夹**（逻辑分组，无按钮）/ 🔺 **图层**（2 按钮）/ 🎨 **样式**（按 site_status 分色图例，无按钮）。Site 按 运营商 → 类别（存量/规划/勘测）→ 类型 分层；Road / Lessor 为与 Site 平级的独立图层根（无运营商划分）。图层节点两按钮：**[导入图层]**（按导入方式**强制盖戳** operator/category/type，忽略源属性，将错就错，保留 F13 清洗 → F4 冲突两步向导）/ **[查看图层要素]**（浮动列表框，本版**只读** + 本层**筛选** + 点击定位，虚拟化）。规划类 Macro NP / Micro NP 画 50m 透明**辐射圈**（仅渲染不入库）。渲染 = **图层图标形状 × site_status 颜色**。顶部全局 [📁 导入] 按钮移除，导入入口下沉到图层。详见「图层体系（F20 · V1.x #24）」节 |
| F19 | 审计日志（V1.x #23）| 记录关键操作 **12 类**（open / import / export_full / export_region / export_conflicts / restore_point_create_auto / restore_point_create_manual / restore_point_delete / restore_point_rollback / restore_point_undo_last_import / clear_baseline / **audit_log_export**）；**身份识别 = IP + User-Agent + Session ID**（V1 不做登录、不做域账号 — 浏览器限制）；**隐藏入口：连续按 3 次 `Esc`** → 弹密码框 → 输入 `mangosv5` → Modal 表格（倒序时间 + 操作类型筛选 + 分页 50/页 + **导出 Excel**）；密码不限错误次数；**UI 只读**（无删除/编辑），**右上角 [💾 导出 Excel]** 按当前筛选结果导出；后端不开 `DELETE`/`PATCH` 端点；**永久保留**；详见「审计日志（F19）」节 |

---

## UI 布局

### 整体布局（方案 D）

```
┌──────────────────────────────────────────────────────────────────────┐
│ [💾 导出 KMZ ▾] [⬛ 框选 ▾] [🔍 ___] [🔄] [🗑️ 清除基线]   (导入下沉到图层) │ ← 顶 Toolbar 56px
├──────────────────────────────────────────────────────────────────────┤
│ 📍 基线国家：菲律宾 (PH) · 固化于 2026-05-29                          │ ← 基线状态栏 28px (V1.x #15)
├──────────────┬─────────────────────────────────┬─────────────────────┤
│ 🔍 搜索...   │                                 │  📋 属性面板        │
│              │                                 │                    │
│ 📂 Site (8)  │                                 │  当前：Site V2152  │
│  ☑ V2231     │                                 │  ─────────────     │
│  ☑ V2249     │      🗺️  OpenLayers 地图        │  PROJECT: ...      │
│  ☑ V2152     │                                 │  SITE ID: V2152    │
│ 📂 Road (1)  │  右上：缩放 / 定位 / 底图切换    │  OPTION: -         │
│  ☑ Reclamat. │  右下：比例尺 / 鼠标坐标         │  LATI: 10.341766   │
│ 📂 Lessor(4) │                                 │  LONGI: 123.8971   │
│  ☑ Mall ...  │                                 │  ...（全字段平铺） │
├──────────────┴─────────────────────────────────┴─────────────────────┤
│ ▾ 12:34:56 导入 sample.xlsx → 成功，新增 3，冲突 5    🟢 DB 已连接  │ ← 折叠条 28px
└──────────────────────────────────────────────────────────────────────┘
```

#### 全局基线状态栏（V1.x #15 · F15）

- **位置**：Toolbar 下方独立一条 28px 横条
- **背景**：浅灰底 (`#f8f9fa`)，accent 色边框 1px
- **文案模式**：
  - 已确立：「📍 基线国家：菲律宾 (PH) · 固化于 2026-05-29」（emerald 文字）
  - 未确立：「📍 基线未确立 · 首次导入完成后将自动锁定」（灰字）
- **数据来源**：单次 `SELECT * FROM baseline_state LIMIT 1`，启动 + 每次 commit 后刷新
- **点击行为**：V1 不做（V2 候选：点击显示基线详情 popup）

**宽度分配（默认）**：左树 20% / 中地图 55% / 右属性 25%。属性面板默认隐藏，选中要素后展开。底部输出面板默认折叠（28px），展开 200px。

**面板可拖拽缩放**：左树、右属性、底部输出三个面板的边缘有 **6px 拖拽条**（三个面板统一宽度，常驻边框色、hover/拖动时变 accent 绿），鼠标变成 `↔` / `↕` 光标时按住可调整大小；拖动时地图区自动 invalidate 重绘以匹配新尺寸。**拖动重绘经 `requestAnimationFrame` 节流**（避免 13000 要素下每像素一次 `updateSize` 导致拖动卡顿）。缩放范围：

| 面板 | 最小 | 最大 |
|------|------|------|
| 左树 | 200px | 500px |
| 右属性 | 240px | 600px |
| 底部输出（展开态） | 120px | 500px |
| 底部输出（折叠态） | 28px（固定） | — |

各面板缩放后的尺寸通过 `localStorage` 持久化，下次打开恢复。

**默认值策略（渐进式）**：
- **初次访问**（无 localStorage 值）→ 按 CSS 默认百分比渲染（左 20% / 右 25%），跨视口响应式
- **用户首次拖动后** → 该面板尺寸切换为 px 并写入 localStorage
- 之后该面板永远按 localStorage 的 px 值渲染，不受视口大小影响
- 清空 localStorage → 回到响应式百分比默认

### 顶部 Toolbar

| 按钮 | 操作 |
|------|------|
| ~~📁 导入文件~~ | **【V1.x #24 · F20 移除】** 顶部全局导入按钮取消；导入入口下沉到每个 🔺 图层节点的 [导入图层] 按钮（"导入一定发生在图层上，不在文件夹上"）。单文件/单选/100MB/几何护栏/F13→F4 两步向导等规则不变，详见 F20 |
| 💾 导出 KMZ ▾ | 下拉：[导出整库] / [导出选区]（"导出选区"在未画选区时灰禁） |
| ⬛ 框选 ▾ | 下拉：[自由多边形] / [矩形]；进入绘制状态，鼠标变成十字 |
| 🔍 搜索 | 全局搜索框（右上角）；**回车触发**，三类要素全搜（Site/Road/Lessor，匹配各自显示名，与左树过滤同口径）→ 结果列入底部 Output「搜索结果区」+ **自动飞到第一条**（详见 F16 / 「底部输出面板·搜索结果区」）|
| 🔄 刷新 | 重载库数据 + 重连 DB 状态指示 |
| 🗑️ 清除基线 | **危险操作 + 换基线唯一通道**：弹确认 modal → 后端 truncate `site` / `road` / `lessor` + **`baseline_state`** 四表；主基准随之重置；V1 不做权限控制（仅靠红色按钮 + 确认 modal 防误点）；**执行前自动建恢复点（F17 · reason=pre_clear）**|
| 🕘 恢复点 | 打开**恢复点对话框**（F17）：列表展示最近 10 个恢复点（时间 · 原因徽标 · 三类点数摘要 · 基线国家 · [回滚] [删除]）；顶部 [+ 手动建恢复点] + [↩ 撤销上一次导入]（无 pre_import 点时灰禁）|
| EN/中 | **界面语言切换（F18）**：英文 ↔ 中文切换，偏好存 localStorage；仅切 UI 文案，数据内容不动 |

### 左侧树（V1.x #24 · F20 重构 · 固定深层图层体系）

> 原 F7 浅树（三平铺文件夹 + 13000 站点节点 + 顶部搜索框 + 树虚拟化）**作废**，替换为以下固定深层图层树。详见「图层体系（F20 · V1.x #24）」节。

- **无顶部树搜索框**（树是固定骨架，要素检索改由列表框「筛选」+ F16 右上「搜索」承担）
- **三种节点**：📁 文件夹（无按钮，逻辑分组；**V1.x #35：运营商/类别节点显示子树要素总数**）/ 🔺 图层（[导入图层] [查看图层要素] 两按钮）/ 🎨 样式（按状态分色图例，无按钮）
- **类别英文文案（V1.x #35）**：存量类别英文 `Legacy` → `Existing`（中文"存量"不变）
- **树节点对齐重构（V1.x #36）**：统一三种节点行的图标尺寸与缩进，修"叶子节点缩进跑到父节点前面"+ 图标忽大忽小不对齐：
  - **根因**：🎨 样式行缺展开符占位（FolderRow/LayerRow 首列是 16px 展开符，StyleRow 直接是 checkbox）→ 样式行整体左移、叶子 checkbox 缩回对准父图标；且 📁 emoji 彩色尺寸不可控，展开符/checkbox/站型字符/圆点五种图标尺寸基线各异。
  - **统一行结构**（各列 `flex-shrink:0` 固定宽）：`[缩进 paddingLeft = BASE + depth×INDENT] [展开符 16px] [checkbox 统一尺寸] [类型图标 16px 居中] [label flex:1] [count 靠右]`。
  - **展开符 → lucide**：可展开节点用 `ChevronDown`(展开)/`ChevronRight`(折叠)；**叶子（🎨 样式行）补 16px 空占位**保持列对齐。
  - **文件夹图标 → lucide**：📁 emoji 改 `Folder`(折叠)/`FolderOpen`(展开)，灰色描线 16px；从 label 文字抽到独立类型图标列。
  - **类型图标列统一 16px 居中**：FolderRow = lucide Folder、LayerRow = 站型字符（▲●■… #29）、StyleRow = 圆点（style-dot）。
  - **checkbox 统一尺寸**（固定 width/height + flex-shrink:0），三行一致。
  - **缩进基准统一**：三行统一 margin-left / box-sizing / paddingLeft 公式，层级阶梯清晰（子比父右移一级，不再跑前面）。
  - 全树图标统一灰色描线，与 #29 站型字符 / #32 lucide 按钮图标族风格一致；FolderRow 的分组标题文字样式（uppercase/muted）可保留作层级区分，但图标列与缩进必须对齐。
- **层级**：Site → 运营商（Globe/Smart/Dito）→ 类别（存量/规划/勘测）→ 站型图层 → 状态样式；Road / Lessor 为与 Site 平级的图层根
- 各节点带复选框：📁 文件夹三态控后代、🔺 图层控全层、🎨 样式控该状态子集显隐
- **单要素不进树**：点 🔺 图层的 [查看图层要素] → 浮动列表框（虚拟化）展示该层要素；点列表行 → 地图飞到该要素 + 属性面板（复用 F12）
- **地图→树 反向定位**：地图选中要素时，左树自动展开并高亮其所属 🔺 图层 / 🎨 样式节点
- 面板宽度可拖拽（右边缘拖拽条，范围 200–500px，`localStorage` 持久化）
- 右键菜单：**V1 不做**

**性能 · 虚拟化重心转移**：树节点固定约 100 个，**不再需要虚拟化**。原 13000 节点的虚拟化压力转移到「查看图层要素」列表框——列表框用固定行高虚拟化渲染（DOM 只保留可视区 + overscan），按目标行号计算 `scrollTop` 居中滚入定位。

### 中间地图

- 引擎：**OpenLayers**
- 底图：默认 CartoDB Positron；右上角控件可切「OSM」「Esri 卫星」「Google 卫星（测试用）」
- 地图控件：
  - 右上：缩放 +/- 按钮、定位（fit bounds 到全部数据）按钮、底图切换
  - 右下：比例尺 + 当前鼠标经纬度
- 要素样式（**V1.x #24 · F20：形状来自图层站型、颜色来自状态**）：
  - Site：**形状 = type**（Macro ▲ / Micro ● / IBS ■ / Macro NP △ / Micro NP ○ / Macro-ongoing ◆ / Micro-ongoing ◇，形状为默认建议可微调）× **颜色 = site_status**（positive 绿 / negative 红 / undermine 黄 / 空值灰）**【V1.x #33：negative 与 undermine 红黄对调】**
  - Lessor：Unfriendly 红面 / Normal 黄面（**去掉 Friendly 绿面**）
  - Road：棕色线
  - 规划类（Macro NP / Micro NP）额外画 50m 透明辐射圈（仅渲染不入库，详见 F20）
- 交互层级：**L2（只查不改） + 框选绘制**
  - 点击要素 → 属性面板显示
  - 框选模式下绘制临时多边形/矩形 → 完成后可导出

### 右侧属性面板

- 默认隐藏；选中要素后从右侧滑入
- 顶部：要素类型 + 名称（如 "Site: V2152" / "Lessor: Mall of Asia"）
- 内容：**所有字段平铺**列出
  - Site：50 列全展示
  - Lessor：4 列全展示
  - Road：1 列全展示
- 关闭按钮：右上角 ✖
- **关闭后无常驻入口**：再次打开靠点击地图要素或左树节点；点击新要素时面板自动展开 + 切换到新要素内容
- 面板宽度可拖拽（左边缘拖拽条，范围 240–600px，`localStorage` 持久化）

### 底部输出面板

- **折叠态**（默认）：高 28px，显示最新一条消息 + 右侧 DB 状态圆点
  - 搜索后折叠态显示汇总行：`🔍 搜索匹配 N 条，飞到第一条`（可点 → 展开面板并飞到第一条）
- **展开态**：高 200px，含：
  - 📊 进度条：导入/导出/解析时显示百分比
  - 🔍 **搜索结果区**（见下）：与操作日志**并列的独立区块**，不计入 50 条日志
  - 📜 操作日志：时间戳 + 消息（最近 50 条）
  - ⚠️ 错误警告：红色高亮显示
  - 🟢 DB 连接状态：左下角圆点（绿正常/红断开）
- 操作：点折叠条切换展开/收起；右上角 [清空日志]（**只清日志，不动搜索结果区**）
- 展开态高度可拖拽（上边缘拖拽条，范围 120–500px，`localStorage` 持久化）

#### 搜索结果区（F16）

- **触发**：右上角 🔍 搜索框回车
- **匹配规则**：三类要素全搜（Site / Road / Lessor），匹配各自**显示名**（site_id+option / road property / lessor name），大小写不敏感、子串包含——与左树过滤框同口径
- **结果呈现**（一行一条）：
  - 区头：左侧 `🔍 搜索匹配 N 条，飞到第一条`（可点 → 飞到第一条）；**右侧 [✖ 清空结果] 按钮**
  - 其下每条结果一行，格式 = **显示名 · 核心属性 …… 类型角标**；点击任意一条 → **地图飞到该要素 + 左树该节点同步高亮并滚动定位**（复用 F12 双向焦点同步）
  - **核心属性（填满宽输出框留白，一行平铺）**：
    - Site：`状态: <SITE STATUS> · 项目: <PROJECT> · (lat, lng)`
    - Lessor：`类别: <Lessor Category> · 关系: <Relationship>`
    - Road：`起点: (lat, lng)`（取 LineString 几何首坐标；无则省略）
    - 字段缺失则该项省略，不显示空 `状态: —`
- **清理手段**：区头 [✖ 清空结果] 按钮 → 清空搜索结果区、回到无结果态；**与 [清空日志] 并列、互不影响**（清日志不动结果，清结果不动日志）
- **封顶**：最多列 200 条；超出顶部提示"仅显示前 200 条，请细化关键词"
- **自动飞第一条**：回车后结果非空即自动飞到第一条（= 汇总条默认动作已执行）
- **自动展开**：每次新搜索自动展开 Output 面板，确保结果列表 / 边界文案可见
- **边界文案**：
  - 0 条匹配 → 搜索结果区显示红字"未匹配到任何要素"
  - 空关键词 → 不搜索（搜索框为空时回车无动作）
- **覆盖策略**：新搜索覆盖旧结果（搜索结果区只保留最近一次搜索）

---

## 用户使用流程

### 主流程 1：每周数据维护（1-2 小时）

1. 工程师打开网页 → 看到地图上已有数据
2. 拿到本周新数据：Google Earth 手工标的 KMZ + ISDP/eSurvey 导出的 Excel（多份，**逐个导入**）
3. 点 [📁 导入] → 选**第一个**文件（V1 一次只能一个）
4. 系统解析 → 底部进度条显示 "解析中..."
5. **进入两步导入向导**（单 modal，顶部 step 进度条：[1. 数据清洗] → [2. 冲突检测] → [完成]）

   **步骤 1：数据清洗（F13）**
   - 后端扫描脏数据 4 类（详见「数据清洗规则」节），返回清洗列表
   - 列：类型 / 名称 / 来源文件名 / 问题原因 / 当前坐标 / 修复后预览（如适用）/ 默认动作 / 操作
   - 默认动作按"先入为主"分配：写反 → [自动修复]、漏小数点 → [丢弃]、在海里 → [丢弃]、**不在主基准 → [丢弃]**（野蛮粗暴）
   - **极端保护**：基线已确立但本文件 0 个点位在基线国家境内 → 清洗向导顶部**红色警告 banner**："本文件 0 个点位在基线国家 PH 内，按当前决策导入将一无所获。要换基线请用 [🗑️ 清除基线]"
   - 每行单条按钮（写反 = 3 个选项 `[自动修复] [原样保留] [丢弃]`；其他 3 类 = 2 个选项 `[强制保留] [丢弃]`）
   - 行底色随决策变化：绿 = 自动修复 / 黄 = 保留 / 灰 = 丢弃
   - 顶部 banner（两层信息）：
     - **第 1 行 · 基准区域**：「基准区域：菲律宾（来自基线 14/14 个点，100%）」或「基准区域：菲律宾（来自本文件 14/17 点位，82% · 首次导入将固化为基线）」
     - **第 2 行 · 文件解析摘要**：「文件解析 234 个 site；**文件内重复自动去重 12 组**（保留后者，丢弃 11 个）；待清洗 223 个，检测异常 X 条」
   - 顶部批量按钮：[全部自动修复] / [全部保留] / [全部丢弃] / [按默认动作重置]
   - 底部 [取消导入] / [下一步：冲突检测 →]

   **步骤 2：冲突检测（F4）**
   - 经过清洗后还活着的点参与冲突检测（被丢弃的点不算冲突）
   - 列：类型 / 名称 / 库里现状 / 导入文件 / **来源文件名** / 操作
   - 每行单条 [覆盖] [忽略] 按钮；**点击后行底色立即反馈**（黄=已选覆盖 / 白=保持忽略）
   - 顶部批量 [全部覆盖] / [全部忽略] / [反选]
   - 底部 [← 返回步骤 1] / [取消（导出冲突 Excel 自己改）] / [确认导入]

6. 工程师在两步里逐条或批量决策（**只修改 UI 状态，不触发入库**）→ 步骤 2 点 [确认导入]
7. 系统执行清洗修复（自动 swap 写反坐标）+ 丢弃决策 + 冲突合并 + 入库（**事务点**：[确认导入] 才是真正的数据库写入时刻）→ 底部日志显示 "导入 X.xlsx → 成功，清洗修复 N，丢弃 M，新增 K，覆盖 L"
8. 处理下一个文件 → 重复步骤 3-7
9. 全部导入完成 → 地图自动 fit bounds 到新增数据

### 主流程 2：派工（5-10 分钟）

1. 工程师打开网页
2. 搜索或拖拽地图，定位目标区域（如 "Pasay City"）
3. 点 [⬛ 框选 ▾] → 选自由多边形或矩形
4. 在地图绘制选区（多边形：点击设顶点 → 双击结束；矩形：按住拖拽）
5. 选区完成 → [💾 导出 KMZ ▾] → [导出选区]
6. 系统筛选**严格包含**在选区内的所有 Site / Road / Lessor → 打包 KMZ → 浏览器下载
7. 工程师手动把 KMZ 邮件发给勘测员，邮件正文说明任务
8. 勘测员收邮件 → Google Earth 打开 KMZ → 看到已勘测点位 + 租户区 → 出发避开重复

### 主流程 3：整库备份（按需）

1. 工程师点 [💾 导出 KMZ ▾] → [导出整库]
2. 系统打包整库要素 → 下载 KMZ

> 注：F17 恢复点解决的是"误操作秒级回退"（导入前/清除前自动建点 + 一键回滚），与本流程的 KMZ 导出（对外交付/离线归档）、`DEPLOY.md` 的 `pg_dump`（机器级容灾）三者分工不同，互补不替代。

---

## 应用场景

### 场景 1：每周数据刷新
> 周五下午，工程师小李收到本周新增数据：自己在 Google Earth 标记导出的 KMZ（12 个候选点）、ISDP 导出的 Excel（30 个站点详情）、eSurvey 导出的 Excel（8 个租户面）。小李打开本平台，地图显示上周已入库数据。点 [导入] → 选 ISDP 的 Excel → 弹冲突列表显示 5 条与库重复 → 小李对照看：3 条 ISDP 数据更新（[覆盖]），2 条库里更对（[忽略]）→ [确认导入]。重复处理另外两个文件。整体维护约 1-2 小时。

### 场景 2：紧急派工
> 周一早上，工程师小李接到通知"客户要在马尼拉市 Pasay 区做 5 个候选站点的现场勘测"。小李打开平台，搜索框输入 "Pasay" 自动飞到该区。点 [框选 ▾] → 选自由多边形 → 沿目标街区绘制选区 → 双击结束 → [导出 KMZ ▾] → [导出选区] → 下载 `Pasay_2026-05-28.kmz`。小李把 KMZ 附件邮件给勘测员小王，正文写"重点勘测空白区域，已标记的避开"。小王 Google Earth 打开 KMZ → 看到已勘测点位（绿/红/黄 pin）和租户区 → 出发只勘测新点。派工流程 10 分钟。

### 场景 3：Excel 列名不匹配
> 工程师小李导入一份 eSurvey 导出的 Excel，但该批数据列名是 `Site_ID` 而非标准 `SITE ID`。系统解析失败，底部输出面板红色警告："导入 eSurvey_2026-05-25.xlsx 失败：必填字段 SITE ID 找不到（检测到候选列：Site_ID）"。小李回去手工把 Excel 列名改成 `SITE ID` → 重新拖拽导入 → 成功解析。**V1 不做 AI 自动识列**，把责任压回用户。

---

## 补充说明

### 去重规则

| 类型 | 主键字段 | 备注 |
|------|----------|------|
| Site（点） | `SITE ID` + `OPTION` 组合 | 大小写不敏感、去首尾空格、按字符串比较；**全局去重**（跨图层/运营商，非按层） |
| Lessor（面） | `fid` | 同上 |
| Road（线） | `Property` | **V1.x #24 起按 Property 去重**（原"不做去重"作废） |

### 合并策略

| 来源 | 策略 |
|------|------|
| 同一文件内重复 | **后者自动覆盖前者**，无需用户确认；**清洗向导顶部 banner 第 2 行显示去重摘要**让用户知道丢了什么 |
| 同一批次多文件内重复 | **后者自动覆盖前者**（与"同一文件内"相同规则；解析阶段 dict 折叠后再与库对照） |
| 文件 vs 库 | **弹冲突列表强制处理**：覆盖 / 忽略仅导无冲突 / 取消导出冲突 Excel 自行修改 |

字段合并方式：用户选"覆盖"时，新数据**完整替换**库中旧记录（V1 不做字段级合并）。

### 数据库设计

PostgreSQL 16 + PostGIS，**业务表 3 张 + 状态表 1 张 + 地理数据表 1 张**：

| 表 | 主键 | 核心强类型列 | 扩展字段 | 几何字段 |
|----|------|------------|---------|---------|
| `site` | `(site_id, option)` 联合 | PROJECT / SITE STATUS / SITE ID / OPTION / LATI / LONGI / **operator / category / type**（V1.x #24 · F20 新增三列，导入时由图层盖戳）| `extras JSONB`（Excel 50 列扩展统一存此） | `geom POINT SRID 4326` |
| `road` | 自增 `id` | Property（V1.x #24 起为去重键）| — | `geom LINESTRING SRID 4326` |
| `lessor` | `fid` | Lessor Name / Lessor Category / Relationship（V1.x #24 起仅 Unfriendly/Normal 两态）| — | `geom POLYGON SRID 4326` |
| **`baseline_state`**（V1.x #15 新增）| `id INT CHECK (id = 1)` 单行约束 | `iso_a2` / `name_zh` / `coverage_pct` / `points_used` / `established_at` | — | — |
| `countries`（地理数据，docker init 时加载）| 来自 Natural Earth | `iso_a2` / `iso_a3` / `name` / `name_zh` | — | `geom MULTIPOLYGON SRID 4326`（GIST 索引）|
| **`restore_point`**（V1.x #20 · F17 新增）| 自增 `id` | `reason`（pre_import/pre_clear/pre_rollback/manual）/ `note` / `created_at` / `site_count` / `road_count` / `lessor_count` / `baseline_iso_a2`（摘要列，列表直接展示免反查）| — | — |
| **`site_snapshot` / `road_snapshot` / `lessor_snapshot` / `baseline_state_snapshot`**（V1.x #20 · F17 新增）| 各自镜像源表列 + `restore_point_id`（外键 ON DELETE CASCADE）| 源表全列副本 | 同源表 | 同源表 |
| **`audit_log`**（V1.x #23 · F19 新增）| 自增 `id BIGSERIAL` | `ts` / `session_id` / `ip` / `user_agent` / `action`（11 类枚举）/ `result`（success/failed）/ `error_msg` | `details JSONB`（按 action 不同字段不同）| — |

**字段集策略（两层）**：
- **强类型核心列**：KML 公共字段直接建列，承担去重主键、索引、空间查询
- **`extras JSONB` 扩展列**：Excel 的 50 列长尾字段（联系人、合同、技术规格等）统一塞入 JSONB，避免硬编码 50 列导致表结构脆弱；后续不同来源 Excel 列扩展，JSONB 直接容纳，无需迁移
- **解析时 `extras` 必须排除核心强类型列**（如 `SITE ID` / `OPTION` / `LATI` / `LONGI` 等），避免属性面板出现"同字段重复显示"
- Lessor 的 `extras` 排除集合**同时包含规范拼写和历史拼写错误**（`Lessor Category` 与 `Lessor Cagegory` 都要排除）
- 属性面板渲染顺序：先展示强类型列，再展开 `extras` JSONB 的 key-value 对

**批量空间查询性能边界**：
- 清洗判定（如 `classify_points`）的 PostGIS `ST_DWithin / ST_Distance` 等批量查询，**必须用 `unnest($1::text[], $2::float8[], $3::float8[])` 数组传参**
- 不能用 `VALUES ($1, $2, $3), ($4, $5, $6), ...` 这种逐参数 placeholder：PostgreSQL 单次查询参数上限约 65535，asyncpg 实际限制 32767，导入大批量点位（如 10000+ Excel 行）会触发 `too many parameters`

**主基准缓存原则**：
- 主基准国家 **一次性固化** 到 `baseline_state` 表，**不在 import 流程中动态计算**
- 禁止重复触发 `_country_dist_in_db` 对整张 `site` 表做空间查询（之前实测 13000 行 ≈ 30 秒，是 V1.x #15 之前的主要性能瓶颈）
- 详见「主基准区域算法」节的状态机

**镜像选型**：`postgis/postgis:16-3.4`（不是 `pgvector/pgvector:pg16` — 本项目要 PostGIS 空间扩展，不要向量扩展）

### 图层体系（F20 · V1.x #24）

**动机**：F7 的浅树（Site / Road / Lessor 三个平铺文件夹 + 13000 个站点节点）无法表达"哪个运营商、哪种类别、哪种站型"的业务分类。工程师需要按 **运营商 → 类别 → 站型** 逐层组织、按层导入、按层查看。本节定义替代 F7 的固定深层图层体系。

#### 设计前提与本版取舍（重要 · 一句话讲清这版在做什么）

- **固定分类、写死**：菲律宾市场 = **Globe / Smart / Dito** 三家运营商，**不会增加**；类别（存量/规划/勘测）、站型（Macro/Micro/IBS/...）也写死在树里。树是**固定骨架（~100 个节点）**，不随数据动态生长。
- **本版模型 = 按导入方式强制盖戳，忽略属性，将错就错**：用户点某个图层的 [导入图层] → 文件里的点**强制归属**到该图层（operator / category / type 三个值由图层节点盖戳写入），**源数据里这三个属性一律忽略**，不做智能校正。"一个人把 Dito 导进了 Smart，他自己承担，平台不替他修。"
- **导出忠实回放 = 空的就导出空的**：导出严格按库里实际存的值回放，**不智能补全、不修正**。之前空就空，有啥导出啥。
- **导入一定发生在图层上，不会在文件夹上**：📁 文件夹是纯逻辑概念无按钮；只有 🔺 图层有 [导入图层]。
- **单要素不再进树**：树最深只到 🎨 样式层（按状态分色的图例）；具体要素（13000 个站点）**全部移到「查看图层要素」浮动列表框**里（虚拟化重心从树转移到列表框）。
- **本版不提供单要素删除**（太复杂，软删除 + 回收站留到 V2）；**导错了的兜底 = F17「撤销上一次导入」**整批回滚。
- **下一步（独立需求）**：列表框提供**编辑要素属性**能力，是未来"数据驱动树"模型的基石（见「未来方向」）。

> **本版与未来方向的分界**：本版是"忽略属性、按导入盖戳、将错就错"的实用版；未来理想态是"树位置由属性派生、数据与表现一致、导入挂根节点、改属性即重新挂载节点"。理想态记录在「未来方向」，**本版不实现**。

#### 三种节点类型

| 图标 | 类型 | 按钮 | 复选框 | 说明 |
|------|------|------|--------|------|
| 📁 | **文件夹** | 无 | 三态（控制后代显隐） | 纯逻辑分组：Site 根、各运营商、各类别；**运营商 / 类别节点右侧显示子树要素总数（V1.x #35：该分组下所有叶子要素计数，非直接子节点数；与图层/样式节点计数同源 `siteMap` 口径）** |
| 🔺 | **图层** | **2 个**（[导入图层] / [查看图层要素]；**V1.x #27：紧跟节点文字后、hover 该节点行才显示、平时隐藏**） | 有（控制整层显隐） | 一个 SITE TYPE（站型）= 一个图层；导入与查看的唯一入口；显示该层要素总数 |
| 🎨 | **样式** | 无 | 有（控制该状态子集显隐） | 图层下按 site_status 分色的图例（positive 绿 / negative 红 / undermine 黄 + 灰 fallback，#33 起红黄对调）；**固定挂载、0 要素也显示**（#33）；显示该状态要素数；**不含要素节点**，要素在列表框 |

#### 完整树结构（固定骨架）

```
📁 Site                              ← 文件夹（根）
├─ 📁 Globe                          ← 文件夹（运营商）
│  ├─ 📁 存量                        ← 文件夹（类别 category）
│  │  ├─ 🔺 Macro            [导入][查看]  ← 图层（type=Macro）
│  │  │   ├─ 🎨 positive 🟢
│  │  │   ├─ 🎨 negative 🟡
│  │  │   └─ 🎨 undermine 🔴
│  │  ├─ 🔺 Micro            [导入][查看]
│  │  └─ 🔺 IBS              [导入][查看]
│  ├─ 📁 规划                        ← 文件夹（类别）
│  │  ├─ 🔺 Macro NP         [导入][查看]   ← 含 50m 辐射圈
│  │  └─ 🔺 Micro NP         [导入][查看]   ← 含 50m 辐射圈
│  └─ 📁 勘测                        ← 文件夹（类别）
│     ├─ 🔺 Macro-ongoing    [导入][查看]
│     └─ 🔺 Micro-ongoing    [导入][查看]
├─ 📁 Smart   … （同 Globe 子结构）
└─ 📁 Dito    … （同 Globe 子结构）

🔺 Road      [导入][查看]            ← 图层根（与 Site 平级，无运营商划分）
│  └─ 🎨 (棕线，单样式)
🔺 Lessor    [导入][查看]            ← 图层根（与 Site 平级，无运营商划分）
   ├─ 🎨 Unfriendly 🔴
   └─ 🎨 Normal 🟡
```

- **类别 → 站型映射（写死）**：存量 = {Macro, Micro, IBS}；规划 = {Macro NP, Micro NP}；勘测 = {Macro-ongoing, Micro-ongoing}。
- 每个 Site 图层下固定挂 3 个状态样式节点（positive/negative/undermine）；**空值/未知状态的点渲染为灰**，归入隐式「未分类 🎨 ⚪」（仅当存在空值点时显示）。
- **样式节点是固定骨架，不随计数消失（V1.x #33）**：🎨 样式节点代表「样式类目」，是图层的**固定子骨架**——**即使该样式下 0 要素也照常显示**（显示计数 0 + 复选框 + 色点），不因空而隐藏。所以**图层节点永远有子节点、永远可展开**。
  - Site 图层：固定挂 **positive / negative / undermine** 三个样式节点（恒显示，即使全 0）。
  - **灰色「未分类 ⚪」例外**：它是 catch-all（site_status 空值或区配不上 pos/neg/und 的点才归入），**仅当存在这类点时才显示**，不固定常驻。
  - Road 图层：固定挂 **🎨 棕线** 单样式（恒显示）。
  - Lessor 图层：固定挂 **🎨 Unfriendly 红 / Normal 黄** 两样式（恒显示）。
  - **撤销 V1.x #32 改动1**：#32 当时给"空层"做的「disclose 显灰 `−` 不可点（leaf 态）」是**建立在样式节点被错误隐藏之上的错误补丁**——根因是 `StyleRow` 写了 `ids.length===0 return null` 违背本节"固定挂"原意。#33 修正后图层永远可展开，#32 leaf 逻辑作废回退（disclose 恢复始终 `+`/`−` 可点）。#32 其余三条（空层 [查看] 置灰 / 两按钮图标化）**保留不变**。
- **树搜索框移除**：原 F7 顶部树搜索框删掉（树是固定骨架，无需搜索）；要素检索改由列表框「筛选」+ F16 右上「搜索」承担。
- 节点总数固定约 100 个，**不再需要树虚拟化**（虚拟化转移到列表框）。
- **图层节点图标（V1.x #29）**：🔺 图层节点图标**按站型形状渲染**（▲Macro / ●Micro / ■IBS / △Macro NP / ○Micro NP / ◆Macro-ongoing / ◇Micro-ongoing；Road 图层根 = 线形 ▬、Lessor 图层根 = 面形 ◼），**统一灰色**（与地图形状一致、便于树图对照）；图层含多状态点，图标只表形状不表状态，**状态分色仍在下面 🎨 样式节点**。原统一 🔺 emoji 作废（#24 完整树结构里的 🔺 仅为示意）。
- **图层节点交互细化（V1.x #32）**：四条收尾，去掉误导、空层禁用、按钮图标化：
  - ~~**空层不显示 `+`（无子节点 → leaf 态）**：cnt=0 时 disclose 显灰 `−` 不可点~~ **【V1.x #33 作废回退】**：此条基于"样式节点被隐藏"的错误前提；#33 改样式节点为固定骨架后，图层永远有子、永远可展开，disclose 恢复始终 `+`/`−` 可点（详见上「样式节点是固定骨架」条）。
  - **空层 [查看图层要素] 按钮置灰**：cnt=0 时 view 按钮 `disabled`（灰 + not-allowed），空层查看无意义；cnt>0 才可点。导入按钮不受要素数影响（空层正是要导入的）。
  - **[导入图层] 按钮改图标**：纯图标（**lucide `Download`**，箭头入托盘——导入 = 数据向下进入当前界面的通用惯例），去掉文字；i18n 全称移到 `title`（hover tooltip）+ `aria-label`（无障碍）。
  - **[查看图层要素] 按钮改图标**：纯图标（**lucide `Eye`**，view/preview 通用惯例），去掉文字；title/aria-label 同上。
  - 图标方案：引入 `lucide-react`（描线单色 SVG，stroke=currentColor 自动随主题/hover 由灰变 accent），与 #29 灰色图标族风格统一；两按钮仍保持「hover 整行才显示」（#27-2）不变。
- **选中态独立色（V1.x #34）**：「选中」高亮统一改用**独立蓝色 `--selected: #3b82f6`**，不再复用全局 `--accent`（emerald 绿 `#10b981`）——绿选中与 positive 绿要素撞色。覆盖**同一 selectedId 的三个视图**：
  - 🌳 树：`.node.selected` / `.layer-row.node-highlighted` / `.style-row.node-highlighted`（左边条 + 文字 + 淡蓝垫底 + 蓝辉光）
  - 🗺️ 地图：选中要素描边 `--feat-selected-stroke` 改指 `--selected`（MapView 经 `cssVar()` 读 CSS 变量，自动跟，不改 MapView.tsx）
  - 📋 列表框：`.lfl-row.selected`（左边条 + 淡蓝垫底）
  - 新增 `--selected`（主题无关，放 :root）+ `--selected-bg`（淡蓝垫底，暗/亮各一）+ `--selected-glow`（蓝辉光，亮色 none）；**全局 `--accent` 主题色及 hover 态的 `--accent-bg` 一律不动**——只换"选中态"，不动悬停/按钮/主题。选中蓝避开所有状态色（绿 positive / 红 negative / 黄 undermine / 棕 road）。

#### 数据模型变更

**`site` 表新增 3 列**（强类型核心列）：

| 列 | 取值 | 来源 | 备注 |
|----|------|------|------|
| `operator` | Globe / Smart / Dito | **导入时由图层盖戳**（忽略源属性） | 写死三家 |
| `category` | 存量 / 规划 / 勘测 | 导入时由图层盖戳 | 写死三类 |
| `type` | Macro / Micro / IBS / Macro NP / Micro NP / Macro-ongoing / Micro-ongoing（= SITE TYPE） | 导入时由图层盖戳 | = 图层名 |

- **`site_status` 复用**（不新增列）：positive / negative / undermine，承担 🎨 样式分色；空值 → 灰 fallback。
- **去重键不变**：Site 仍按 `SITE ID + OPTION` **全局**去重（跨图层、跨运营商，不是按层去重）。导入到某层时若该键已存在于库内任意位置 → 进 F4 冲突。
- **Road 改为按 `Property` 去重**（**V1 原"不做去重"作废**）。
- **Lessor 样式收窄**：relationship 仅 **Unfriendly / Normal** 两态，**去掉 Friendly**（fid 去重不变）。

#### [导入图层]（按钮 1 · 强制盖戳导入）

1. 点某 🔺 图层的 [导入图层] → 弹**单文件**选择器（沿用 F1：`.kml`/`.kmz`/`.xlsx`，一次一个，100MB 上限）。
2. **几何护栏（前置）**：图层是强类型的——Site 图层只收点、Road 图层只收线、Lessor 图层只收面。文件里几何类型不匹配的要素 → **跳过 + 在底部输出窗口报告**（"跳过 N 个非点要素"），不阻断其余导入。
3. **盖戳**：通过护栏的要素，`operator/category/type` **强制写为该图层的值**，**源文件里的这三个属性一律忽略**（将错就错）。`site_status` 仍读源数据（决定落入哪个 🎨 样式）。
4. **沿用两步向导**：F13 数据清洗 → F4 冲突检测，逻辑不变（清洗规则、主基准判定、冲突覆盖/忽略/取消导冲突 Excel 全部照旧）。
5. **全局去重**：冲突检测仍按全局键（Site = SITE ID+OPTION / Road = Property / Lessor = fid），不因分层而局限于本层。
6. **顶部全局 [📁 导入] 按钮移除 + 地图拖拽导入禁用（V1.x #28）**：导入唯一入口 = 图层的 [导入图层]（"导入一定在图层上"）。**地图拖拽文件不再触发导入**——拖拽会绕过盖戳 + 几何护栏，产生 operator/category/type 全 NULL 的脏数据，与盖戳模型冲突；拖文件进地图区 → 提示"请用图层的 [导入图层] 按钮"，不导入。

#### [查看图层要素]（按钮 2 · 浮动列表框）

- 点某 🔺 图层的 [查看图层要素] → 弹**浮动列表框**（floating panel），以**多列表格（带固定表头）**列出该层全部要素。
- **列集（带表头，V1.x #27 补）**：
  - Site 图层：`PROJECT` / `SITE ID` / `OPTION` / `SITE STATUS` / `LATI` / `LONGI` / `OPERATOR` / `CATEGORY` / `TYPE`
  - Road 图层：`Property`（+ 起点经纬度，取 LineString 首坐标）
  - Lessor 图层：`Lessor Name` / `Lessor Category` / `Relationship`
  - 表头随列表滚动固定（sticky header）；虚拟化保持（固定行高、DOM 只渲可视区）
- **本版只读**（无编辑/删除按钮）；**编辑能力是下一步独立需求**。
- **筛选（非搜索）**：列表框自带筛选框，**只筛本图层**内的要素（精确叫"筛选"；右上 F16 那个才叫"搜索"，搜全库三类）。
- **点击行 → 地图定位**：点任意一行 → 地图飞到该要素并高亮（复用 F12 双向焦点同步 + 属性面板显示）。
- **虚拟化**：列表框承接原树的大数据量（单层可能数千行），固定行高虚拟化渲染，DOM 只渲可视区。
- **浮动窗口交互（V1.x #30）**：列表框是一个**可拖、可缩的固定尺寸浮动窗口**，不再随行数变高：
  - **弹出位置**：关闭状态点图层 [查看] → 窗口靠近该触发节点弹出（点哪从哪弹）；**已打开时点另一图层 [查看] → 同窗口切换内容、位置和尺寸保持不动**（单窗口复用现有 viewLayer 单 state，首次靠近节点、之后切层不乱跳）
  - **固定尺寸窗口**：默认 840×460（宽够 Site 9 列一屏不横滚）；内容多 → body 区**内部虚拟化滚动 + 可见滚动条**，窗口大小不变（不再"高度随行数变"）；空层仍是固定窗口 + "暂无要素"
  - **可拖动**：抓 header 拖动挪位置（挡地图就拖开），**限制至少保留 header 在视口内**（防拖到完全看不见）；拖动改位置走 `requestAnimationFrame` 节流（防 13000 要素下掉帧）
  - **可缩放**：右下角缩放手柄（复用 `ResizeHandle`），最小 480×320、最大视口 − 32px 边距
  - **尺寸持久化**：宽高存 localStorage（key 如 `presurvey.lfl.w/h`，参照 #11 面板套路）下次恢复；**位置不持久化**（每次首次靠近触发节点，会话内拖动有效）
  - **层级**：z-index 600，低于导入向导 / 审计 Modal（1000）——被 Modal 盖住是对的（Modal 优先）
  - **眼睛按钮 toggle 开关（V1.x #34）**：图层 [查看图层要素]（👁 Eye）按钮是**开关**——点 👁 弹列表框；**再点同一图层的 👁 → 关闭**列表框。判定"同一图层" = `target`（kind/operator/category/type）全等：相等则关（set viewLayer = null），不等则切换到新层（保持 #30 同窗口换内容）。提升易用性（同一按钮开/关，不必去点窗口的 ✖）。实现：`onViewLayer` 接 `toggleLayerFeatures(target, anchor)`，按当前 `viewLayer.target` 与点击 `target` 是否相等决定关/开切。✖ 关闭按钮（`closeLayerFeatures`）保留不变。
  - **表格随窗口宽度自适应（V1.x #31）**：表格列宽**跟随窗口宽度伸缩填满**，不再固定列宽、右侧留白：
    - 各列按其基准宽度（Site 9 列 / Road / Lessor 各自列集的 `col.w`）**等比拉伸**占满内容区（窗口越宽列越宽，基准宽大的列分得多）
    - 表格有**最小宽度 = 该列集各列基准宽之和**（Site≈802 / Road=400 / Lessor=420），**列不压缩到基准以下**
    - 窗口内容区**窄于该最小宽度**时 → body 区出**横向滚动条**，靠滚动看全列（这正是 #30「没滚动条」之外的另一半：纵向虚拟滚 + 横向按需滚）
    - 窗口 `MIN_W`(480) 与表格最小宽**各自独立**：窗口可缩到 480，表格仍保最小宽、溢出部分横滚（窗口比表格窄是允许的）
    - sticky 表头、虚拟化行、选中行随列宽一起伸缩（表头/行/虚拟层宽度统一 = `max(totalW, 内容区宽)`）

#### 样式与渲染（形状 × 颜色）

- **形状来自图层（站型 type）**，**颜色来自状态（site_status）**，二者叠加 = 地图上的最终图标（如"绿色三角""黄色五角星"）。
- **颜色（site_status）**：positive = 🟢 绿 / negative = 🔴 红 / undermine = 🟡 黄 / 空值未知 = ⚪ 灰。**（V1.x #33 起 negative 红、undermine 黄；单一真源 `utils.STATUS_COLOR`）**
- **形状（type）映射（V1.x #25 · 已与用户敲定，接受默认）**：实施侧用 OpenLayers `RegularShape` 渲染（points/radius/angle 控形状，fill/stroke 控实心/空心）。逻辑：**实心 = 存量、空心 = 规划、菱形 = 勘测**。

  | type | 建议形状 |
  |------|---------|
  | Macro | ▲ 实心三角 |
  | Micro | ● 实心圆 |
  | IBS | ■ 实心方 |
  | Macro NP | △ 空心三角 |
  | Micro NP | ○ 空心圆 |
  | Macro-ongoing | ◆ 实心菱形 |
  | Micro-ongoing | ◇ 空心菱形 |

- Road = 棕色线（单样式，不分状态）；Lessor = 面（Unfriendly 红面 / Normal 黄面）。
- 🎨 样式节点的复选框控制"该层该状态子集"在地图上的显隐；🔺 图层复选框控全层；📁 文件夹三态控后代。

#### 50m 辐射圈（规划类专属）

- **Macro NP / Micro NP** 两个规划图层的点，在地图上额外绘制一个 **50m 半径透明圆**（辐射范围示意，**不是选区**）。
- **仅渲染、不入库**（不写任何几何到 DB），**50m 固定**（不可配）。
- 其余图层（存量/勘测/Road/Lessor）不画圈。

#### 筛选 vs 搜索（口径区分）

| 能力 | 入口 | 范围 | 性质 |
|------|------|------|------|
| **筛选** | 「查看图层要素」列表框内的筛选框 | **仅本图层** | 在已打开的单层列表里收窄 |
| **搜索** | F16 右上角搜索框 | **全库三类**（Site/Road/Lessor） | 真正的全局检索，结果进底部输出搜索结果区，不变 |

#### 配色与状态迁移（实施需处理的存量数据）

- **V1 旧 legend "Positive 绿 / Negative 黄 / Unknown 红"** → 新口径 **positive / negative / undermine**：**`Unknown` 状态值迁移为 `undermine`**（仅状态值改名，数据层 `_norm_site_status` 不变）。**配色 V1.x #33 起调整：negative = 红、undermine = 黄（与旧 legend 的红黄分布不同，以 #33 为准）。**
- 空值 / null 状态 → 灰 fallback（未分类）。
- 旧 Lessor `Friendly` → 迁移为 `Normal`（去掉 Friendly 一态）。
- 旧 site 数据**没有 operator/category/type**（在浅树时代导入）：**V1.x #25 拍板 = 清库重来，不做库内迁移**。现有库数据为开发/测试数据，F20 代码上线后走 F14「清除基线」truncate `site`/`road`/`lessor` + `baseline_state`，再按新图层树逐层 [导入图层] 重新灌入（盖戳写三列）。**不新增「未分类」兜底层，固定骨架保持 Globe/Smart/Dito 纯净。** 注意：库内值迁移虽免，但**导入器层面**源文件状态值规范化仍保留（源 KML 可能仍含 `Unknown`/`Friendly`，导入时映射到 `undermine`/`Normal`）。

#### 实施硬约束（必须连带处理，否则数据/回滚出错）

1. **F17 快照链路同步**：`site` 加 3 列后，**`site_snapshot` 表结构 + `restore_point_helper.py` 建点 `INSERT ... SELECT` + `restore_points.py` 回滚 SQL** 三处的显式列清单都要同步加 `operator/category/type`，否则回滚会静默丢这三列数据（回滚后分层全空）。Road 加 Property 去重不动列，Lessor 去 Friendly 仅影响值不影响列。
2. **`extras` 白名单扩展**：新增 3 列要并入 `_SITE_CORE` 解析白名单（与 SITE ID/OPTION/LATI/LONGI 同列），避免属性面板"同字段重复显示"。
3. **状态值迁移脚本**：`site_status` 旧值 `Unknown` → `undermine`；Lessor `Friendly` → `Normal`（幂等 migration）。
4. **虚拟化重心转移**：树降到 ~100 节点不再需虚拟化；列表框接管大数据量虚拟化。
5. **几何护栏 + 盖戳**：导入器按目标图层类型校验几何并强制写 operator/category/type，忽略源属性。

#### 本版边界与未来方向

- **本版不做**：单要素删除（→ V2，软删除 + 回收站，多人撤销栈复杂，需先想清保留步数与并发）。
- **下一步（独立需求）**：列表框**编辑要素属性**。
- **未来方向（仅记录，不实现）**："数据驱动树"——节点位置由属性派生，数据与表现一致：在站点根节点导入（无属性挂根）；改运营商属性 → 挂到对应运营商；改状态 → 挂到对应状态；按站点情况自动挂到对应图层。本版反其道而行（忽略属性、按导入盖戳），是实用折中，将来再演进到理想态。

#### 反转声明（推翻两轮前的锁定 · 遵循 #8 反转纪律）

本节明确**推翻**需求讨论中两轮前锁定的「节点不改属性 / 空的导出空的（不盖戳）」结论：

- **旧锁（作废）**：导入节点不改要素属性，要素归属由其自身属性决定。
- **新锁（本版生效）**：**按导入方式强制盖戳 operator/category/type，忽略源属性，将错就错**。
- **保留并兼容的部分**：导出仍"空的就导出空的"忠实回放——但忠实回放的对象是**盖戳后库里实际存的值**（不是源文件值），二者不再冲突。

### 基线恢复点与回滚机制（F17 · V1.x #20）

**动机**：`清除基线`（F14）不可逆、坏导入会污染累积数据，菲律宾现场出错没退路。F17 是这两类不可逆操作的**安全网**。设计参考专业软件的两层做法——**操作级撤销 + 命名恢复点/快照**（不上 Esri 版本控制/PITR 那种重型方案，本工具数据小、内部单点用）。

**"基线"的完整定义**（一个恢复点要快照的内容）：`site` + `road` + `lessor` 三张业务表 **+ `baseline_state`**（主基准固化态）。四者一起才是一个可回滚的完整基线。

**自动建点触发（三处，都在对应写操作之前、同一事务内）**：
- **导入 commit 前** → reason=`pre_import`（"撤销上一次导入" = 回滚到最近的 pre_import 点）
- **清除基线（F14）前** → reason=`pre_clear`
- **回滚执行前** → reason=`pre_rollback`（**回滚本身也先建点 → 回滚可逆，防手滑**）

**手动建点**：Toolbar [🕘 恢复点] → 对话框 [+ 手动建恢复点]，reason=`manual`，可填 note。

**存储**：DB 内镜像表（见「数据库设计」表）。建点 = `INSERT ... SELECT` 把三表 + baseline_state 整体拷进 `*_snapshot`，挂 `restore_point_id`。数据小（万级行），成本可忽略。

**保留策略**：环形，**保留最近 N=10 个**，超出删最旧（`restore_point` 删行 → 快照行 `ON DELETE CASCADE` 连带清理）。V1.x 不做"锁定/置顶/分类配额"，统一按时间淘汰，保持简单。

**回滚语义（覆盖式）**：选中某恢复点 → **二次确认弹窗（destructive 红）** → 事务内 `TRUNCATE site/road/lessor` + 清 `baseline_state` → 从该点的 `*_snapshot` 整体重灌 → 重置主基准为快照里的 `baseline_state`。回滚前自动建 `pre_rollback` 点。

**API**：
- `GET /api/restore-points` → 列表（仅元数据，不含快照明细）
- `POST /api/restore-points` → 手动建点 `{note?}`
- `POST /api/restore-points/{id}/rollback` → 覆盖式回滚（内部先自动建 pre_rollback）
- `DELETE /api/restore-points/{id}` → 删除某点（CASCADE 清快照）
- 撤销上一次导入 = 前端定位最近 `pre_import` 点 → 调其 rollback

**与冷备份的分工（重要，不重复造轮子）**：恢复点是 **DB 内**快照，**卷（`presurvey_pgdata`）丢了恢复点一起没**。真正的容灾仍靠 `DEPLOY.md` 的 `pg_dump` 冷备（异地）。两者互补：恢复点管"日常误操作秒级回退"，pg_dump 管"机器/卷级灾难"。

### KML / KMZ 处理

- 导入：KMZ 是 KML 的 ZIP 压缩包，后端先解压（找 `doc.kml` 或首个 `.kml`）再解析
- 导出：始终输出 KMZ；KML schema 与示例 `Integrated_Libraries_*.kml` 保持兼容（Site / Road / Lessor 三个 Schema）
- 导出 KMZ 内置 7 个 Style 定义（point-green / yellow / red、poly-green / yellow / red、line-brown），与示例文件一致，保证 Google Earth 渲染效果
- **解析时按 `<SchemaData schemaUrl="#site|#road|#lessor">` 区分类型；schema 缺失时按几何类型兜底**（Point→site / LineString→road / Polygon→lessor）
- **`extras JSONB` 展开规则**：导出 KMZ 时 `extras` 字段全部展开到 `<ExtendedData><SchemaData>`，不丢字段；`<Schema>` 节的字段定义取**所有同类要素 `extras` 字段集的并集**（union），保证不同来源的扩展列都被纳入
- **自反一致性契约**：本平台导出的 KMZ 重新导入回本平台时，必须 **100% 命中冲突**（同类型已去重要素全部识别为重复）。这是导入器和导出器之间的核心契约，保证数据"出去"和"回来"格式完全等价。任何破坏此契约的改动都视为回归 bug
- **导出文件命名规则**：`export_full_YYYYMMDD_HHMMSS.kmz`（整库） / `export_region_YYYYMMDD_HHMMSS.kmz`（选区）

### 字段名兼容（已知历史拼写错误）

| 原始数据中的字段名 | 规范字段名（数据库 / Spec） | 来源 |
|------------------|--------------------------|------|
| `Lessor Cagegory`（错拼） | `Lessor Category` / `lessor_category` | 示例 KML `Integrated_Libraries_*.kml` |

解析器导入时必须做这层映射，保证存库字段统一为规范拼写。导出 KMZ 时**也使用规范拼写**，逐步纠正历史数据。

### 解析期异常（解析失败直接拒绝，不进清洗向导）

| 异常 | 检测方式 | V1 处理 |
|------|---------|---------|
| Excel 列名不匹配（如 `Site_ID` vs `SITE ID`）| 必填字段缺失 | 解析失败，输出面板红色提示，列出检测到的候选列名 |
| KML schema 标签缺失或不规范 | `<SchemaData>` 没有 `schemaUrl` | 按几何类型兜底分类（Point→site / LineString→road / Polygon→lessor） |
| 文件超过 100MB | 前端 / 后端 / nginx 三端校验 | 前端直接拦下；后端 HTTP 413 |
| 网络断开 / 导入中途失败 | 数据库事务异常 | 导入事务回滚，库不脏 |

### 数据清洗规则（F13 · 进清洗向导让用户决策）

解析成功后，扫描 4 类脏数据，按下表分配默认动作并展示给用户：

| 异常 | 检测条件 | 默认动作 | 可选操作 |
|------|---------|---------|---------|
| **坐标写反**（LAT/LONG 颠倒）| LATI 绝对值 > 90 但 LONGI 在合法范围 / 或两者交换后都合法 | **[自动修复]** | [自动修复] / [原样保留] / [丢弃] |
| **坐标漏小数点**（无法自动修）| LATI 绝对值 > 90 且不属于写反场景 / LONGI 绝对值 > 180 | **[丢弃]** | [强制保留] / [丢弃] |
| **在海里** | 1km buffer 内不在任何国家陆地（详见「底层地理数据」节 SQL）| **[丢弃]** | [强制保留] / [丢弃] |
| **不在主基准区域** | 在某国陆地 1km buffer 内但不在基线国家陆地 1km buffer 内 | **[丢弃]**（野蛮粗暴 · 强约束"先入为主"，跨境点须用户主动 [强制保留]）| [强制保留] / [丢弃] |

**自动修复语义**：

- 坐标写反 → 入库时 swap LATI / LONGI 字段值（`extras` 里同步反过来）
- 其他类型 V1 不做自动修复

### 主基准区域算法（"先入为主" · **一次性固化，永不重算**）

主基准是 **一次性事件**：基线第一次确立时算一次，固化进 `baseline_state` 表，**之后所有导入都直接读，永不重新计算**。这是为了：

1. **避免基线漂移**（10 + 20 + 13000 个点连续导入，若每次重算会让基线被最后那批数据"夺权"）
2. **彻底解决性能问题**（之前每次导入都要扫整张 site 表，13000 行 → 30 秒）
3. **强约束"先入为主"语义**：核心数据先入库，杂数据再补

#### 状态机

```
未确立 (baseline_state 空表)
    │ 第一次 commit 成功 + site 表新增 > 0
    │ 算 _country_dist_in_db (基于刚入库的 site)
    │ 若返回非 None → INSERT INTO baseline_state
    ↓
已确立 (baseline_state 有 1 行)
    │ 后续 commit：什么也不做，baseline_state 不变
    │ F14 清除基线：TRUNCATE baseline_state + site + road + lessor
    ↓
未确立 (回到初始)
```

#### 三种使用场景

| 时机 | 读取方式 | 性能 |
|------|---------|------|
| 导入开始时检查基线 | `SELECT * FROM baseline_state LIMIT 1` | ~1ms |
| 清洗判定 `not_in_baseline` | 单行 SELECT 拿 `iso_a2` 然后传给 `classify_points` | ~1ms |
| 全局基线状态栏渲染 | 同上 | ~1ms |

#### 极端情况

- **首次入库点全在海里 / 全无国家**：`_country_dist_in_db` 返回 None → `baseline_state` 不写入 → 状态保持"未确立"，下次导入仍是 current_file 模式
- **已确立后导入文件全部不在基线国家**：清洗向导默认全部 [丢弃]；**顶部红色警告 banner**："本文件 0 个点位在基线国家 PH 内，按当前决策导入将一无所获。要换基线请用 [🗑️ 清除基线]"
- **已确立后用户主动 [强制保留] 不在基线国家的点**：这些点入库但 `cached_country_iso_a2` ≠ 基线 → 后续清洗仍以基线为准（不会因为这些"杂种点"动摇主基准）

#### 清洗向导 banner（仍保留 + 极端保护）

- **基线未确立**：「基准区域：菲律宾（来自本文件 14/17 个点，82% · 首次导入将固化为基线）」
- **基线已确立**：「基准区域：菲律宾（已固化 · 来自基线 14/14 个点，100%）」
- **基线已确立 + 本文件 0 点在基线国家**：上方红色警告 banner（雷 29 选项 B）

### 底层地理数据（单表统一判定 + 1km buffer 容错）

| 数据集 | 用途 | 加载方式 |
|--------|------|---------|
| **Natural Earth `ne_10m_admin_0_countries`** | 国家陆地多边形（**同时**判"在海里"+"不在主基准国家"）| docker init 时一次性加载到 PostGIS 表 `countries`，关键字段：`iso_a2` / `iso_a3` / `name` / `name_zh` / `geom` |

体积约 30MB（随仓库 `deploy/db/geo_data` 提供 SQL dump 或 GeoJSON），CC0 公共领域 + 离线，不依赖外部 API。

**判定改成单表统一逻辑（避免两数据集间矛盾 + 容错小岛）**：

```sql
-- "在海里"判定
NOT EXISTS (SELECT 1 FROM countries WHERE ST_DWithin(point.geom, countries.geom, 0.01))
-- 0.01 度 ≈ 1.1km 近赤道。1km buffer 容错 Natural Earth 10m 分辨率漏掉的小岛/礁石

-- "不在主基准"判定
EXISTS (SELECT 1 FROM countries WHERE ST_DWithin(point.geom, countries.geom, 0.01))
  AND NOT EXISTS (
    SELECT 1 FROM countries
    WHERE iso_a2 = :baseline_iso_a2 AND ST_DWithin(point.geom, geom, 0.01)
  )
-- 即：在某国陆地 1km 内，但不在基线国家陆地 1km 内
```

**为什么不用 `ne_10m_land`**：

- Natural Earth `ne_10m_admin_0_countries` 已经覆盖所有有主权的陆地（含小岛）
- 国家边界数据维护通常比纯陆地数据更精细（主权诉求导致岛屿覆盖更全）
- 单表判定避免"两数据集结果矛盾"（如：land 说在海里、countries 说在某国）
- 体积更小

### 外部依赖

- **ISDP** / **eSurvey**：内部勘测系统，无 API，通过 Excel 导出对接
- **Google Earth**：勘测员客户端，本平台输出 KMZ 由此打开
- **腾讯云**：部署环境
- **邮件**：派工通道（**V1 不集成发件**，工程师手动发）

### 审计日志（F19 · V1.x #23）

**动机**：V1 不做登录，但关键操作（导入/导出/清基线/回滚/记录点变更）必须有痕迹，方便追责和复盘。

#### 身份识别 = 方案 E（仅技术信息，不识别真人）

| 字段 | 来源 | 说明 |
|------|------|------|
| `ip` | 后端从 `X-Forwarded-For` / socket 拿 | 真实 IP，nginx 转发要传透 |
| `user_agent` | HTTP `User-Agent` header | OS + 浏览器版本 |
| `session_id` | 后端 cookie `presurvey_sid`（首访问时 UUID 写入） | 区分浏览器 session，跨刷新稳定 |

**明确不记录的（V1 受限于浏览器安全）**：Windows 域账号、机器名、MAC 地址、登录用户。

#### 12 类操作（动作枚举）

| action | 触发时机 | `details` JSONB 主要字段 |
|--------|---------|----------------------|
| `open` | session 首次打开页面（`presurvey_sid` cookie 缺失）| — |
| `import` | commit 入库完成 | `file_name` / `parsed_count` / `cleaning_decisions`（自动修复/保留/丢弃各 N）/ `conflict_decisions`（覆盖/忽略各 N）/ `inserted_count` / `restore_point_id`（关联自动建的点）|
| `export_full` | 整库导出 KMZ 完成 | `file_name` / `counts: {site, road, lessor}` |
| `export_region` | 选区导出 KMZ 完成 | `file_name` / `counts: {site, road, lessor}` / `mode: "polygon" \| "rect"` |
| `export_conflicts` | 冲突 Excel 导出完成 | `file_name` / `conflict_count` |
| `restore_point_create_auto` | 自动建点（commit 前 / 清基线前 / 回滚前）| `restore_point_id` / `reason: "pre_import" \| "pre_clear" \| "pre_rollback"` / `counts` |
| `restore_point_create_manual` | 手动建点 | `restore_point_id` / `note`（用户填的备注）/ `counts` |
| `restore_point_delete` | 删除恢复点 | `restore_point_id` / `reason_at_create` / `counts_at_create` |
| `restore_point_rollback` | 回滚到某点 | `restore_point_id` / `new_restore_point_id`（回滚本身建的点）/ `counts_before` / `counts_after` |
| `restore_point_undo_last_import` | F17 快捷撤销上次导入 | 等价 `restore_point_rollback`，但 `details.shortcut: true` |
| `clear_baseline` | F14 清除基线 | `counts_before: {site, road, lessor, baseline_state}` / `restore_point_id`（关联自动建的点）|
| `audit_log_export` | 审计日志 Modal [💾 导出 Excel] 点击后导出完成 | `filters: {action, from, to}` / `exported_rows: N` / `file_name: "audit_log_YYYYMMDD_HHMMSS.xlsx"` |

**明确不记录的（噪音）**：切换底图 / 切换语言 / 框选 / 缩放 / 点击树节点 / 属性面板查看。

#### 隐藏入口（F19 · 雷 C）

- 全局键盘事件监听：**连续按 3 次 `Esc`**（间隔 < 1 秒）→ 弹密码框
- 密码框 placeholder："请输入审计密码"
- 密码硬编码：`mangosv5`（V1 不做密码哈希、不做轮换、不做用户管理）
- 密码错误：抖动 + "密码错误" 提示；**不限错误次数**（V1 简化）
- 验证通过 → 打开审计日志 Modal

#### Modal UI

- 形态：全屏遮罩 + 居中 Modal（70vw × 80vh），跟 RestorePointDialog 风格一致
- 顶部：标题 "审计日志" + [✖ 关闭]
- 顶部筛选条：[时间范围: 全部 / 今天 / 7天 / 30天 / 自定义] + [操作类型: 全部 / 单选枚举]
- 主体：表格列 = 时间 ↓ / 操作 / 详情摘要 / IP / 用户代理 / Session ID 前 8 位 / 结果
- 时间倒序（**新的在上**）
- 分页：50 条 / 页，底部分页器
- 行点击：展开显示完整 `details` JSON
- **只读 + 可导出**：无删除 / 编辑 按钮；**右上角 [💾 导出 Excel] 按钮**
- **导出范围**：**当前筛选结果**（用户能看到什么就导出什么；不能在 Modal 里看的也不能导）
- **导出文件命名**：`audit_log_YYYYMMDD_HHMMSS.xlsx`
- **导出内容**：Excel 列 = 时间 / 操作 / IP / User-Agent / Session ID / 结果 / 错误消息 / details JSON（最后一列原文）
- **导出本身记审计**（**元审计**）：触发 `audit_log_export` action，记筛选条件 + 导出行数

#### 后端约束

- 端点：
  - `GET /api/audit-log?action=&from=&to=&page=&page_size=` — 分页查询
  - `GET /api/audit-log/export?action=&from=&to=` — 导出 Excel（按筛选条件返回 `.xlsx` blob）
- **不开 `POST` / `DELETE` / `PATCH`**（业务自己在 commit / export / clear 等成功路径里通过函数直接写入 `audit_log` 表）
- 失败的操作也要记一条（`result: "failed"` + `error_msg`）
- 永久保留（无 TTL，无清理任务）
- **导出 Excel 端点本身完成后必须再写一条 `audit_log_export` 记录**（**元审计**，防止有人导出大量数据后否认）

#### 审计写入失败的容错

写 `audit_log` 失败 **不应该让业务回滚**（如导入入库成功但审计日志写失败，业务事务仍 commit）。审计写入用独立连接 / try-catch 兜底，失败仅打 `WARNING` 到 stdout，不抛出。

#### 实施补充决策（V1.x #23 · 雷 38-40）

落地阶段在三处补齐定义，避免后续歧义：

**雷 38 · IP 透传链路**

- nginx 必须传 `X-Forwarded-For` 给 api（用 `$proxy_add_x_forwarded_for` 追加 `$remote_addr` 到已有 XFF 链）
- 后端 `_ip_of()` 优先级：`X-Forwarded-For[0]` → `X-Real-IP` → `request.client.host`
- 真实生产中，nginx 前若再加 LB / CDN，需在 LB 侧也开启 XFF 透传

**雷 39 · `open` 仅在 GET 上记录**

- `presurvey_sid` cookie 缺失时，**只有 GET 请求**会触发 `action=open` 审计
- 不在 POST / DELETE / PATCH 上记 `open`（这些请求自带业务审计，重复无意义）
- 极端场景：用户首次访问就是 POST（如脚本直传）→ 不记 `open`，但会记业务 audit，session_id 同样写入 cookie

**雷 40 · `restore_point_create_auto` 与业务审计双条并存**

`commit_import` / `clear_baseline` / `rollback` 三个路径会同时写**两条** audit：

1. 业务 audit（`import` / `clear_baseline` / `restore_point_rollback`）
2. `restore_point_create_auto`（关联自动建的点）

**保留双条的理由**：

- 业务和恢复点是两个独立维度（业务关心"发生了什么"，恢复点关心"快照血缘"）
- 查 audit 时按 action 筛选互不污染（看清基线记录不会被自动建点干扰）
- 关联通过 `details.restore_point_id` 串起（不需要靠 audit 行序）

`restore_point_rollback` 与 `restore_point_undo_last_import` 二选一互斥（取决于回滚目标的 `reason` 是否 = `pre_import`），不会同时记。

---

## 技术栈

| 层 | 选型 | 说明 |
|----|------|------|
| 前端 | React + OpenLayers | 类 QGIS 风格 GIS 应用，OpenLayers 是 QGIS Web 同源 |
| 后端 | Python + FastAPI | 利用现有 Python 经验 |
| 数据库 | PostgreSQL 16 + PostGIS | PostGIS 处理几何字段和空间查询（选区相交判定等） |
| 文件解析 | `fastkml` / `pykml`（KML） + `openpyxl`（Excel） | Python 生态成熟方案 |
| 部署 | Docker + 腾讯云 | **`[待落地对齐]` 参考 mango-memory 的容器编排和数据库实例配置** |
| 派工通道 | 邮件（外部）| V1 不集成发件，工程师手动发 |

---

## V1 边界声明（不做的事）

| 类别 | V1 不做的事 |
|------|------------|
| 认证 | 用户账号 / 鉴权 |
| 工作流 | **基线 / 个人空间双工作区**（Git-like staging area） |
| 协同 | 多人并发冲突处理 / 操作回滚 / 历史版本 |
| 数据 | ~~Road（线）去重~~（**V1.x #24 起已做，按 Property 去重**）；单要素删除（→ V2 软删除 + 回收站）|
| AI | 列名识别 / 坐标修正 / 模糊去重 |
| UI | 点聚合 / 树右键菜单 / 属性 tabs / 地图编辑要素 |
| 通信 | 邮件自动发送 |

未来候选见下文「V2 候选功能」。每次变更由 `Product-Spec-CHANGELOG.md` 记录。

---

## V2 候选功能

V1 不做、未来视情况引入。每项随时间评估优先级。

### 工作流类
- **基线 / 个人空间双工作区**（Git-like staging area）
  - 工程师所有导入/编辑操作只影响个人空间副本，与基线对比 diff，确认后批量同步（commit）到基线
  - 动机：① 避免频繁写后端库；② 提交前可视化对比变更
  - 设计待定：个人空间存储形态（前端 IndexedDB vs 后端 workspace 表）、身份识别方式、同步冲突解决、派工导出从哪个空间
- **多人协同 + 冲突解决**：操作日志、并发锁、回滚
- **认证 / 鉴权 / 账号体系**：与个人空间和多人协同配套
- **历史版本**：基线数据的时间快照
- **工作空间（一空间 = 一国家）**（V1.x #15 用户提出）
  - 当前 V1 是"一个数据库 = 一个国家"，换基线必须 F14 清除
  - V2 允许同时维护多个国家的勘测库：菲律宾空间 / 印尼空间 / 越南空间 ...
  - 顶部基线状态栏改为下拉切换空间
  - 每个空间独立的 `baseline_state` + 各自的 `site`/`road`/`lessor`（schema 隔离或 tenant_id 字段）
  - F14 清除按空间维度

### AI 能力类
- **列名智能识别**：自动映射 `Site_ID` → `SITE ID`，缓解「场景 3」问题
- **坐标异常 AI 检测**：识别 `7177416` 这类漏小数点的脏数据
- **名称模糊去重**：`"Huawei Technologies"` vs `"HUAWEI TECHNOLOGIES PHILIPPINES INC"` 之类公司名变体
- **冲突智能合并建议**：多人协同时帮工程师快速决策

### UI 增强类
- **单要素删除（软删除 + 回收站）**（V1.x #24 · F20 时明确推迟到 V2）
  - 本版「查看图层要素」列表框只读，不提供删除；导错的兜底是 F17「撤销上一次导入」整批回滚
  - V2 难点（用户已识别）：多人同时打开时撤销栈如何呈现、保留多少步、并发顺序——回收站模型可回避按用户/按顺序的复杂度，优先评估回收站方案
- 树节点右键菜单（删除、导出此要素等）
- 属性面板分 tabs（基本 / 联系人 / 技术 / 租赁）
- 点聚合（OpenLayers Cluster）
- 地图上直接编辑要素（拖动、改属性、删除）
- 邮件自动发送派工 KMZ

---

## V1 交付快照 · 2026-05-28

V1 完整范围按 Spec F1-F12 + UI 五区块 + 数据规则 + V1 边界**全部交付并验证通过**。

| 验收维度 | 状态 |
|---------|------|
| 功能需求 F1-F12 | ✅ 12/12 |
| UI 五区块（顶 toolbar / 左树 / 中地图 / 右属性 / 底输出） | ✅ 全部按 Spec 实现 |
| 数据规则（去重、合并、字段名规范化、坐标异常、KMZ 自反一致性） | ✅ 全部验证通过 |
| V1 边界声明的"不做的事" | ✅ 全部确认未越界 |

**实施分四个 Stage 累积完成**：

- **Stage 1**：骨架 + F1（导入） + F2（解析） + F3（冲突检测） + F6（地图） + F7（树骨架） + F12 部分（fit bounds）
- **Stage 2**：F4（冲突列表 UI 两阶段 session） + F8（属性面板） + F11 部分（进度条状态机） + F12 剩余（树点击飞到）
- **Stage 3**：F9（框选导出） + F10（整库导出） + KMZ 自反一致性 + extras 展开
- **Stage 4**：F5（冲突 Excel 导出） + 顶部全局搜索 + 刷新 + 树搜索过滤 + 节点 checkbox + 文件夹三态 + 底图切换 + 比例尺 + 鼠标坐标 + 清空日志

**本快照之后**：Spec 进入"V1 维护版本"状态。任何变更必须经 `Product-Spec-CHANGELOG.md` 记录。V2 候选功能（见上文「V2 候选功能」章节）随时可启动评估。

详细变更轨迹见 `Product-Spec-CHANGELOG.md` #1 ~ #6。

---

## V1.x 视觉皮肤 · Mint Tech

V1 锁版后的第一次增量：**纯皮肤层，不动功能、不动布局、不动数据规则、不动 API 形状**。薄荷绿 accent + 数据强制等宽字体提供科技感来源。

> 注：本节原为 #8 反转后的 Light 单模式（#7 Dark Pro 因"太黑、地图看不清"被否）。**V1.x #19 起改为双主题：默认 Dark Slate（深石板）+ 亮/暗切换**，下方「主色板」为 Light 值，「模式切换」节为 Dark 值与切换规则。Dark 这次根因修复了 #7 的问题——地图区不参与暗化（见「地图区护栏」）。CSS Variables 体系保留，仅扩成两套值。

### 主色板

| 元素 | 色值 | 备注 |
|------|------|------|
| 应用底色 | `#fafafa` | 不是纯白，柔和 |
| 面板背景（树 / 属性 / toolbar / 输出） | `#ffffff` | 纯白带极轻阴影 |
| 分隔线 | `#e5e7eb`（1px） | 极细 |
| 主文字 | `#0f172a` | 深 slate，不刺眼 |
| 次文字 / 占位 | `#64748b` | 中 slate |
| **Accent 强调**（选中 / 链接 / 主按钮） | **`#10b981`** | emerald-500（薄荷绿） |
| Accent 浅底（选中态底色） | `#ecfdf5` | emerald-50 |
| 警告（坐标异常 banner） | `#f59e0b` | amber-500 |
| 错误（DB 断线 / 解析失败） | `#ef4444` | red-500 |
| 成功（导入 OK） | `#10b981` | 同 accent |
| 冲突列表 ·「覆盖」行底 | `#fef3c7` | amber-100（暖黄） |
| 冲突列表 ·「忽略」行底 | `#ffffff` | 同面板 |

### 底图配套（重要 · 替换 #7 的底图清单）

| 槽位 | V1.x 替换为 |
|------|------------|
| 默认底图 | **CartoDB Positron** ⭐（浅净干净，跟整体调一致） |
| 备选 1 | **OpenStreetMap 默认**（色彩丰富） |
| 备选 2 | **Esri 卫星图**（保留不变） |

### 字体方案

| 用途 | 字体栈 |
|------|------|
| UI（按钮、菜单、树节点、属性 label、对话框） | `Inter`, `SF Pro Display`, system-ui |
| 数据（坐标、SITE ID、JSON 字段、日志、文件名） | `JetBrains Mono`, `Iosevka`, `Menlo`, monospace |

### 几何与微交互

- **圆角**：统一 `6px`（按钮、对话框、面板、输入框、徽章）—— 禁止 12px+ 大圆角
- **面板阴影**：`0 1px 3px rgba(0,0,0,0.05)` —— 轻微深度，禁止重阴影
- **Hover 态**：accent 色 1px 边框 + 80ms 颜色过渡
- **选中态**：accent 色 2px 左边框 + `#ecfdf5` 底
- **进度条**：accent 色横向脉冲

### 科技感细节（必做）

1. **数据展示区强制等宽字体**：所有坐标、SITE ID、JSON 字段值、日志行、文件名、数字计数
2. **数字千分位**：计数文案（如 `14 sites · 3 roads · 4 lessors`）；超过 4 位的数字加 `,`
3. **状态点用纯色圆点**（`●`）+ 极小尺寸（6px），**禁止用 emoji 替代状态指示**（如 DB 状态点、文件夹三态指示器）

### 模式切换（V1.x #19 · 双主题）

**默认 Dark（深石板 Slate）+ 亮/暗切换**。Toolbar 右侧加 ☀/☾ 切换按钮，选择写入 `localStorage`，下次打开恢复；**默认暗色**。

> 注：#7 曾做 Dark Pro 被否，理由是"太黑、地图看不清"。本次回归暗色但**根因修复**：#7 错在把暗色刷到地图上（底图换暗 / 滤镜压暗）；本方案**地图区完全不参与主题切换**，底图恒为亮色 CartoDB Positron，只暗化四周外壳。这是业界公认的"暗壳 + 亮地图"范式（地图 canvas 当内容区，亮底可读性更高），不是妥协。

#### 暗色主色板（Dark Slate · 默认）

遵循业界暗色实践：**表面分层**（每层提亮 5–8% 亮度，做出深度）+ **不用纯黑**（投得出阴影）+ **文字不用纯白**（防高分屏"震动"）。

| 语义令牌 | Dark 值 | Light 值（原 Mint Tech） | 备注 |
|---------|--------|------------------------|------|
| `--bg`（应用底色 / surface-base） | `#0f172a` | `#fafafa` | 深石板蓝，非纯黑 |
| `--panel`（面板 / surface-raised） | `#1e293b` | `#ffffff` | 比底色提亮一档 |
| `--overlay`（弹窗 / 浮层 / surface-overlay） | `#273449` | `#ffffff` | 再提亮一档，浮于面板之上 |
| `--border`（分隔线） | `#334155` | `#e5e7eb` | 细灰蓝 |
| `--text`（主文字） | `#e2e8f0` | `#0f172a` | 调白，非纯白 |
| `--text-muted`（次文字 / 占位） | `#94a3b8` | `#64748b` | 中 slate |
| `--accent`（强调 / 选中 / 主按钮） | `#10b981` | `#10b981` | **薄荷绿两主题共用** |
| `--accent-soft`（选中态底色） | `rgba(16,185,129,0.14)` | `#ecfdf5` | 暗底用半透明绿垫底 |
| `--warning` | `#f59e0b` | `#f59e0b` | amber |
| `--error` | `#f87171` | `#ef4444` | 暗底用亮一档红 |

#### 暗色微交互增量

- **选中 / 激活态加辉光**：`box-shadow: 0 0 8px rgba(16,185,129,0.45)`（薄荷绿在暗底上发光，这是"Fashion"的来源，克制使用：仅选中节点、激活按钮）
- 圆角 / 字体 / 等宽数据区 / 状态圆点等其余几何细节**两主题一致**，不随主题变
- 阴影在暗底改用更深的 `rgba(0,0,0,0.35)`（亮底是 `0.05`）

#### 地图区护栏（硬约束 · 防重蹈 #7）

- 地图 OpenLayers canvas、底图 URL、要素样式（pin / 面 / 线颜色）**完全不参与主题切换**
- 默认底图恒为 **CartoDB Positron（亮）**，三个底图选项不动
- **禁止**对地图容器用 `filter: invert / brightness / hue-rotate` 等任何压暗手段
- 不新增暗色底图（用户已明确：地图保持亮底图）

### 视觉一致性约束（实施硬要求）

- 所有元素**禁止硬编码色值**，必须引用 CSS Variables
- CSS Variables 体系**只换"值"不换"键"**，组件代码理论上零修改
- **双主题实现（#19）**：暗色为默认值，定义在 `:root`（或 `[data-theme="dark"]`）；亮色定义在 `[data-theme="light"]`。两套同名令牌只是值不同。切换 = 改 `<html data-theme>` 属性，`localStorage` 持久化，**默认 dark**
- Toolbar 右侧加 **☀/☾ 切换按钮**（唯一新增控件；其余交互文案、布局、数据规则、API、命名规则、事务边界完全不动）
- 已有组件（Toolbar / LayerTree / MapView / AttributePanel / OutputPanel / ConflictDialog 等）逐项核对，确保无硬编码色漏网
- **地图区不参与主题（见「地图区护栏」）**：MapView / 底图 / 要素样式不动
- 字体加载方式不变
