# ─────────────────────────────────────────────────────────────────────────────
# cloud.conf —— 公有云目标（腾讯云 x86 / compose）（DEPLOY-DESIGN §3 / §4）
# ⚠️ 本批 cloud 子命令为 stub（后续批次 P4 实现），此文件仅占位 + 字段预留。
# ─────────────────────────────────────────────────────────────────────────────

# 架构
ARCH=amd64

# 编排方式：docker compose up
ORCHESTRATOR=docker-compose

# compose 文件（相对项目根 / deploy/prod）
COMPOSE_FILE=deploy/prod/docker-compose.prod.yml

# 腾讯云源站（CF 橙云回源）
HOST=43.163.3.9
SSH_ALIAS=coder

# 镜像来源：本地 build
IMAGE_SOURCE=local-build

# —— DB 连接（migrate 用；通过 docker exec <DB_CTN> psql 连库）——
# 腾讯云测试环境 compose 的 db 容器：库 presurvey / 用户 presurvey。
DB_CTN=presurvey-db
DB_USER=presurvey
DB_NAME=presurvey

# 安全：公有云为测试库，允许清库重来
PROD=false
ALLOW_RESET=true
