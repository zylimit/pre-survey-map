-- ═════════════════════════════════════════════════════════════════════════════
-- V1__example_noop.sql —— 示例迁移（DEPLOY-DESIGN §5.1，端到端验证用）
--
-- ⚠️ 这是一个【无害示例】，仅用于验证版本化迁移机制（schema_migrations 登记 /
--    checksum 比对 / 单事务 / 迁移前恢复点 / 幂等）端到端跑通。
-- ⚠️ 不承载任何真实业务 schema 变更，验证完成后可整文件删除（删除前先确认
--    各环境 schema_migrations 已无需依赖此记录，或同时清掉对应 version=1 行）。
--
-- 内容：给 site 表加一条 COMMENT（纯元数据，不改结构、不动数据、可重复执行）。
-- ═════════════════════════════════════════════════════════════════════════════

COMMENT ON TABLE site IS 'pre-survey-map 点要素表（migrate 机制示例 V1 写入的 COMMENT，可删）';
