# 内网部署环境变量 —— 复制成 .env.prod 并改强密码
# cp .env.prod.example .env.prod && 编辑

DB_USER=postgres
DB_PASSWORD=CHANGE_ME_到强密码
DB_NAME=presurvey

# 宿主映射端口（被占用就改；服务器信息收集脚本会报端口占用）
WEB_PORT=80
API_PORT=8000
DB_PORT=5433
